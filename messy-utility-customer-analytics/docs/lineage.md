# Lineage

Track lineage at the record level using:

- source_system
- source_file
- batch_id
- ingestion_timestamp
- record_hash

Every curated table should retain the raw identifiers and batch metadata needed to trace records back to source files.
